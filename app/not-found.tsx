import { NotFoundCard } from "@/components/match/NotFoundCard";

export default function NotFound() {
  return <NotFoundCard title="Esta página no existe" body="Capaz que el enlace quedó cortado." />;
}
